void main() {
  // Add -> +;
  // Sub -> -;
  // Multiply -> *;
  // Divide -> /;
  // Reminder -> %;
  // Proper decimal -> ~/    (This gives you the proper decimal number remove the number after the decimal);

  /*var a = 5, b = 3;

  var add = a + b;
  print(add);
  var sub = a - b;
  print(sub);
  var mul = a * b;
  print(mul);
  var div = a / b;
  print(div);
  var rem = a % b;
  print(rem);
  var pd = a ~/ b;
  print(pd);
*/

// Incriment and Decriment..
  /*
 1. ++i (incriment first then print the value)
 2. i++ (print the value then incrment the value)
 3. --i (decrment first then print the value)
 4. i-- (print the vaue then decrement)
}
*/

//Logical Operator
//logical AND
/*
1. true && true = true;
2. true && false = false;
3. false && true = false;
4. false && false = false;
*/
//Logical OR;
/*
true || true = true;
true || false = true;
false || false = ture;
false || false = false;
*/
  var lst;

  lst = ["akash", "kumar", "manish"];
  print(lst);
}

